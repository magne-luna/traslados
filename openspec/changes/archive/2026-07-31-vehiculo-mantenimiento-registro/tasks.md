> **Strict TDD activo.** Cada task de código va RED → GREEN → TRIANGULATE → REFACTOR.
> Antes de modificar un archivo existente: correr su suite y registrar el baseline (safety net).
> Verificación de tipos SIEMPRE con `npx tsc -b --noEmit` dentro de `frontend/` — **nunca** `tsc --noEmit` a secas.
> Checkpoint de governance MEDIO: las Open Questions 1, 2 y 3 de `design.md` se confirman con el usuario **antes** de la sección 2.

## 0. Safety net

- [x] 0.1 Correr la suite completa (`npm run test` en `frontend/`) y registrar el baseline de archivos/tests pasando. Si algo falla antes de tocar nada, reportarlo como falla preexistente y **no** arreglarlo dentro de este change. **Baseline**: 171/190 archivos, 1274/1385 tests pasan. 19 archivos fallan (111 tests) por un error preexistente no relacionado (`TypeError: Cannot read properties of undefined (reading 'clear')` en `localStorage.clear()`, con warning de Node `ExperimentalWarning: localStorage is not available because --localstorage-file was not provided` — un global `localStorage` de Node compite con el de jsdom), afecta `mockPacienteRepository`, `mockPresupuestoRepository`, `mockVehiculoRepository`, `AppShell`, `VehiculosRoute`, etc. — ninguno de los archivos que este change toca. Reportado como falla preexistente, no se arregla acá. `src/features/vehiculos/{GastosVehiculo,VehiculoDetail}.test.tsx` pasan limpio en el baseline.
- [x] 0.2 Correr `npx tsc -b --noEmit` dentro de `frontend/` y confirmar 0 errores de arranque. **0 errores.**

## 1. Documentación de la discrepancia (antes de tocar código — regla dura del proyecto)

- [x] 1.1 Agregar entrada en `knowledge-base/04_modelo_de_datos.md` §Discrepancias: `GastoVehiculo.categoria` usaba `'mantenimiento' | 'reparacion' | 'service'`, valores **inventados** (sin fuente en el docx, la KB ni el spec `vehiculo-gastos`); el docx tiene dos entidades separadas (`Gastos de Vehículo`: Vehículo/Monto/Fecha, sin categoría; `Mantenimiento`: Vehículo/Categoría/Fecha/Próximo vencimiento fecha/Kilometraje actual/Próximo vencimiento km); la categoría real es de dos niveles (nivel 1 del docx: gasto | preventivo | correctivo; nivel 2 de US-500). Aclarar que acá **sí** se resuelve el valor correcto porque está confirmado contra el docx, y que lo que queda pendiente de confirmar son las Open Questions 1, 3, 4 y 5 de `design.md`.
- [x] 1.2 Actualizar el bullet `⚠️ Discrepancia con Traslados-Modelo-Datos.docx` de `CHANGES.md` §C-08 (L200-204): tachar/actualizar el punto de la tabla Mantenimiento (este change crea la entidad en el frontend) y sumar los puntos nuevos — categoría de dos niveles, `gasto_vehiculo` sin categoría, `mantenimiento_registro` sin monto, sin FK entre ambas tablas, `descripcion` del gasto como agregado del frontend.
- [x] 1.3 Anotar en `CHANGES.md` §C-08 §"Progreso frontend" que la categorización de gastos/mantenimiento quedó alineada al docx por este change, con el path del change.

## 2. Contrato de tipos (`frontend/src/shared/types/vehiculo.ts`)

- [x] 2.1 Eliminar `CategoriaGasto` y el campo `categoria` de `GastoVehiculo`, dejando `{ id, fecha, monto, descripcion? }`. Documentar en el comentario del tipo que el docx da al gasto solo Vehículo/Monto/Fecha y que `descripcion` es texto libre (Open Question 5).
- [x] 2.2 Definir `TipoIntervencion = 'gasto' | 'preventivo' | 'correctivo'` (los tres valores del docx), con comentario citando el texto literal del campo Categoría.
- [x] 2.3 Definir `SubtipoPreventivo = 'cambio-aceite-filtros' | 'vtv' | 'rto'` (cerrado, US-500 + RN-VE-03/04) y `SubtipoCorrectivoConocido = 'alternador' | 'bateria' | 'frenos' | 'embrague' | 'cubiertas'` + `SubtipoCorrectivo = SubtipoCorrectivoConocido | 'otro'`, con comentario explicando por qué correctivo es abierto (el "etc." de US-500) y por qué la apertura es `'otro'` y no `string`.
- [x] 2.4 Definir `MantenimientoRegistroBase` (`id`, `fecha`, `kilometraje`, `proximoVencimientoFecha?`, `proximoVencimientoKm?`, `descripcion?`) mapeando cada campo al nombre del docx en el comentario. **Sin `monto`.**
- [x] 2.5 Definir `MantenimientoRegistro` como unión discriminada de 4 miembros (preventivo+subtipo / correctivo+subtipo conocido / correctivo+`'otro'`+`detalle: string` / `gasto` sin subtipo), de modo que `tsc` garantice las dos invariantes (design.md D4).
- [x] 2.6 Sumar `mantenimientos: MantenimientoRegistro[]` a la interfaz `Vehiculo`.
- [x] 2.7 Test de tipos: `frontend/src/shared/types/vehiculo.mantenimientoRegistro.types.test.ts`, con casos `@ts-expect-error` que fallan si se construye un correctivo `'otro'` sin `detalle` o un `gasto` con `subtipo`, más 3 casos válidos (preventivo, correctivo `'otro'`+detalle, gasto sin sub-tipo) — RED confirmado (el import de `MantenimientoRegistro` fallaba, `TS2305`, antes de 2.1-2.6), GREEN confirmado (`npx tsc -b --noEmit` 0 errores en el archivo + `npm run test` 5/5). Nota TDD: el `@ts-expect-error` de un objeto multi-línea debe ir pegado a la línea de la propiedad excedente exacta (`subtipo: ...`), no a la línea de apertura del objeto — TS atribuye el error de "excess property" a esa línea puntual, no al `const` de arriba.
- [x] 2.8 Corrí `npx tsc -b --noEmit` y listé los call sites rotos (32 archivos). **Hallazgo no anticipado por design.md/tasks.md**: además de los archivos de `vehiculos` (esperados), 17 archivos fuera del scope declarado en el Impact de `design.md` construyen un `Vehiculo`/`NuevoVehiculo` a mano y rompieron solo por la nueva propiedad requerida `mantenimientos` (nada que ver con `categoria`): `conductores/AsignacionSemanalTabla.test.tsx`, `dashboard/{DashboardAccesibilidad,DashboardPage,RecorridosDelDiaPanel,useAlertasMantenimiento}.test.{tsx,ts}`, `hojas-de-ruta/{AsignacionPanel,HojaDeRutaImprimible,HojaDeRutaPage.coherencia,HojaDeRutaPage,NuevoRecorridoForm,RecorridoCard,VistaGlobalHojaDeRuta}.test.tsx`, `shared/lib/hojas-de-ruta/{capacidadDisponible,disponibilidad,vehiculosCompatibles}.test.ts`, `shared/lib/reportes/{alertasMantenimiento,inmutabilidad}.test.ts`, y (dentro de `vehiculos` pero no listados en Impact) `useVehiculos.test.ts`, `VehiculosList.test.tsx`, `VehiculosPage.test.tsx`. Se corrigieron ya (mecánico, `mantenimientos: []`, gobernanza BAJA — mismo patrón que el `gastos: []` ya presente en cada uno) para no dejar una regresión de compilación fuera de los archivos que este change se propuso tocar. Call sites de `categoria` que quedan pendientes para las secciones 3-7: `categoriaGastoOptions.ts`, `GastosVehiculo.{tsx,test.tsx}`, `VehiculoDetail.{tsx,test.tsx}`, `VehiculoMantenimiento.test.tsx` (solo por `mantenimientos`, ya no por `categoria`), `mockVehiculoRepository.test.ts`, `vehiculosFixture.ts`.

## 3. Catálogos de etiquetas y validación (funciones puras — TDD puro, sin DOM)

- [x] 3.1 RED: `frontend/src/features/vehiculos/mantenimientoCategoriaOptions.test.ts` — confirmado fallando por módulo inexistente antes de crear la implementación.
- [x] 3.2 GREEN: `frontend/src/features/vehiculos/mantenimientoCategoriaOptions.ts` con `TIPO_INTERVENCION_LABELS`, `TIPO_INTERVENCION_CHIP_KIND` (`gasto` → `secondary`, `preventivo` → `success`, `correctivo` → `warning`), `SUBTIPO_PREVENTIVO_LABELS`, `SUBTIPO_CORRECTIVO_LABELS` (incluyendo `'otro'`), `subtiposDe(tipo)` y `TIPOS_INTERVENCION_ALTA = ['preventivo', 'correctivo']`. 5/5 tests verdes.
- [x] 3.3 Borrado `frontend/src/features/vehiculos/categoriaGastoOptions.ts`. `tsc -b --noEmit` confirma 0 importadores rotos por el borrado.
- [x] 3.4 RED: `frontend/src/features/vehiculos/validateMantenimientoForm.test.ts` — 12 casos (fecha, kilometraje obligatorio/numérico/no-negativo, tipo obligatorio, sub-tipo obligatorio en preventivo y correctivo, detalle obligatorio en `'otro'` incluyendo el borde de solo-espacios, correctivo con sub-tipo conocido sin detalle, próximo vencimiento opcional). Confirmado fallando por módulo inexistente.
- [x] 3.5 GREEN: `frontend/src/features/vehiculos/validateMantenimientoForm.ts`. 12/12 tests verdes.
- [x] 3.6 RED→GREEN: `frontend/src/features/vehiculos/toMantenimientoRegistro.ts` + `.test.ts` (5 casos: preventivo sin `detalle`, correctivo `'otro'` con `detalle`, correctivo conocido sin `detalle`, próximo vencimiento presente/ausente). 5/5 verdes. Sin lógica que extraer del componente todavía (se escribe después, sección 6).
- [x] 3.7 Revisado `validateGastoForm.test.ts` / `validateGastoForm.ts`: ninguno de los dos asume `categoria` — sin cambios necesarios.

## 4. Mock y fixture

- [x] 4.1 Safety net: corrido `mockVehiculoRepository.test.ts` — 8/8 fallan por el error de entorno preexistente ya documentado en 0.1 (`localStorage.clear()` indefinido), consistente con el baseline. Ninguno falla por lógica de la aplicación.
- [x] 4.2 `SCHEMA_VERSION` → `3`, motivo documentado junto a la constante. Se agregó el test específico que faltaba (RED→GREEN a nivel de lógica, bloqueado en ejecución por el mismo issue de entorno de 4.1/0.1): `re-siembra si el localStorage tiene gastos con el campo categoria del esquema anterior (schemaVersion 2)` en `mockVehiculoRepository.test.ts` — payload `schemaVersion: 2` con un gasto `categoria: 'reparacion'` y sin `mantenimientos`; con `SCHEMA_VERSION` en 2 (antes del bump) el mismatch no se daba (2 === 2, no re-sembraba); con `SCHEMA_VERSION` en 3 el mismatch sí se da y re-siembra. Verificado por lectura de código y por `tsc` (el payload de prueba no tipa contra `Vehiculo`, es `unknown` vía `JSON.stringify` de un objeto suelto, a propósito — simula el payload viejo real).
- [x] 4.3 `vehiculosFixture.ts`: quitada `categoria` de los 2 gastos sembrados (Etios: "Cambio de cubiertas"; Partner: agregada `descripcion: 'Reparación de frenos'` en vez de la categoría perdida, para no perder la pista de qué era el gasto).
- [x] 4.4 `vehiculosFixture.ts`: sembrados 5 `mantenimientos` cubriendo los 3 valores de nivel 1 y ambos caminos de nivel 2 — Etios: `preventivo`/`cambio-aceite-filtros` (próximo vencimiento por km) + `correctivo`/`'otro'`+`detalle` ("Reemplazo de radiador perforado"); Kangoo: `preventivo`/`vtv` (próximo vencimiento por fecha, señaliza a propósito la duplicación con `habilitaciones` de la Decisión 5); Partner: `correctivo`/`frenos` (conocido) + `gasto` (registro de solo lectura, cubre el camino de lectura sin alta). `tsc -b --noEmit` limpio en ambos archivos.

## 5. UI — quitar la categoría del registro de gastos

- [x] 5.1 Safety net: baseline de referencia = la corrida completa de 0.1 (`GastosVehiculo.test.tsx` pasaba 5/5 ahí); al momento de esta task el archivo ya estaba roto en compilación por el borrado de `categoriaGastoOptions.ts` en la sección 3 (estado intermedio esperado del orden de `tasks.md`, no una regresión nueva).
- [x] 5.2 RED: `GastosVehiculo.test.tsx` actualizado — sin selector de categoría, sin columna Categoría, `onAgregar` sin `categoria`, gateo de escritura sin el campo categoría. Confirmado fallando (el componente todavía importaba `categoriaGastoOptions`, inexistente).
- [x] 5.3 GREEN: `GastosVehiculo.tsx` — quitado `<Field>`/`<Select>` de categoría, estado `categoria`, columna `Th`/`Td` de Categoría, `Chip` e imports de `categoriaGastoOptions`; `NuevoGastoInput` sin `categoria`. 8/8 tests verdes.
- [x] 5.4 REFACTOR: tabla queda con 3 columnas (Fecha, Monto, Descripción), `Table` sin `minWidth` calibrado — no había ancho fijo para 4 columnas que corregir, el componente ya era fluido.

## 6. UI — historial de mantenimiento

- [x] 6.1 RED: `HistorialMantenimiento.test.tsx` (13 casos: vacío, poblada, detalle de 'otro', registro 'gasto', selector de alta sin gasto, sin monto, cascada, detalle condicional, validación, altas válidas x2, gateo de escritura x2). Confirmado fallando (módulo inexistente).
- [x] 6.2 GREEN: `HistorialMantenimiento.tsx` con el layout `grid lg:grid-cols-[2fr_1fr]` de `GastosVehiculo.tsx`, usando `Table`/`Th`/`Tr` + `MantenimientoFila` (Chip con texto del tipo de intervención). No se usó `SectionBadge` — ese patrón es de "Configuración | Vista previa" (ChecklistEditor), no aplica acá (tabla + form de alta, mismo criterio que `GastosVehiculo`).
- [x] 6.3 `MantenimientoFila.tsx`: `textoSubtipo()` muestra `detalle` cuando `subtipo === 'otro'`, cubre los 4 miembros de la unión (`gasto` sin sub-tipo). Test de fixture propio (5.a en `mantenimientoRegistro.types.test.ts`) ya cubre la exhaustividad a nivel de tipos.
- [x] 6.4 Cascada en `NuevoMantenimientoForm.tsx`: `handleTipoChange` limpia `subtipo`/`detalle` y `subtiposDe(tipo)` repuebla el select. 2/2 tests de cascada verdes (triangulado preventivo↔correctivo).
- [x] 6.5 `TIPOS_INTERVENCION_ALTA` en el select de tipo; sin campo de monto en el form. Verificado por test.
- [x] 6.6 Campo `detalle` condicional (`input.subtipo === 'otro'`), validado por `validateMantenimientoForm`. Verificado por test (aparece/exige solo con `'otro'`).
- [x] 6.7 Bloqueo de fecha/kilometraje inválidos verificado; próximo vencimiento opcional verificado (alta sin esos campos pasa).
- [x] 6.8 Gateo de escritura: `CamposSoloLectura` envolviendo el form + `<Button requiereEscritura>`, mismo patrón que `GastosVehiculo.tsx`. 2/2 tests de gateo verdes (tabla legible sin permiso, alta inerte; con permiso, alta operativa).
- [x] 6.9 REFACTOR aplicado desde el inicio (no hubo que revisar después): `HistorialMantenimiento.tsx` (56 líneas) delega la fila a `MantenimientoFila.tsx` (50 líneas) y el form a `NuevoMantenimientoForm.tsx` (157 líneas) — los 3 bajo el límite de ~200. `npx tsc -b --noEmit` limpio en los 3 archivos. 13/13 tests verdes tras 2 rondas de ajuste de queries del test (labels ambiguos "Fecha"/"Kilometraje" colisionaban con los campos de "Próximo vencimiento (fecha/kilometraje)", y los `<option>` del select de tipo/sub-tipo colisionaban con el texto del Chip de la tabla — ambos son colisiones del test, no bugs de producción).

## 7. Wiring en el detalle del vehículo

- [x] 7.1 Safety net: `VehiculoDetail.test.tsx` — baseline 9/9 (vitest no aplica `tsc`, corrió igual con el error de tipos de `mantenimientos` pendiente; se arregló el fixture `etios` sumando `mantenimientos: []` antes de seguir).
- [x] 7.2 `handleAgregarMantenimiento` en `VehiculoDetail.tsx`: genera el id con `generateId('mantenimiento')`, persiste vía `actualizar(vehiculo.id, { mantenimientos: [...vehiculo.mantenimientos, nuevo] })`, mismo `sectionError` que `handleAgregarGasto`. Tests: persistencia (payload exacto) + mensaje de error visible ante fallo del repository.
- [x] 7.3 **Decisión tomada al ver la pantalla**: `<HistorialMantenimiento>` se montó en una `Section label="Mantenimiento" title="Historial de mantenimiento"` **propia**, inmediatamente después de la Section "Service y habilitaciones" — la sección existente ya tenía su propio `AvisoModeloDatos` + `VehiculoMantenimiento` (2 tarjetas de alerta); sumarle además la tabla+form del historial la habría sobrecargado. Mismo `label` "Mantenimiento" en ambas Sections (agrupa visualmente bajo el mismo rótulo lateral sin fusionar el contenido).
- [x] 7.4 `AvisoModeloDatos` de "Service y habilitaciones" reescrito: acotado a lo pendiente (VTV/RTO en `habilitaciones`, kilometraje/último service embebidos en `Vehiculo`, ninguno derivado del historial nuevo), referenciando la Decisión 5 de `design.md` y `C-08`. Verificado por test (`/VTV\/RTO se sigue rastreando en.*habilitaciones/i`).
- [x] 7.5 Revisado el `AvisoModeloDatos` de Gastos: no mencionaba `categoria` ni la entidad Mantenimiento — sigue vigente tal cual (discrepancia de módulo de permisos `facturacion` vs `conductores`, sin relación con este change). Sin cambios.
- [x] 7.6 Test "registrar una intervención de mantenimiento no cambia el chip de estado de service del resumen (Decisión 5)" — compara todas las ocurrencias del chip de estado antes/después del alta, sin cambios. Protege la Decisión 5 de `design.md`.

## 8. Verificación final

- [x] 8.1 Suite completa comparada contra el baseline de 0.1: **176/195 archivos pasan (1319/1431 tests)**, vs. baseline 171/190 (1274/1385). Los mismos **19 archivos** fallan en ambos casos, por el mismo error de entorno preexistente (`localStorage.clear()` indefinido) — **0 archivos nuevos rotos**. Delta de tests: +46 tests nuevos (+45 pasan, +1 falla — es el test nuevo de `mockVehiculoRepository.test.ts` de la sección 4, bloqueado por el mismo issue de entorno, no una regresión de lógica). 0 regresiones fuera de los archivos tocados por este change.
- [x] 8.2 `npx tsc -b --noEmit` → **0 errores** (proyecto completo). `npm run lint` (oxlint) → mismos warnings preexistentes de `only-export-components` en archivos no tocados por este change, **0 errores, 0 warnings nuevos**. Confirmado por grep: sin `any`/`as any` ni `style={{` en ningún archivo tocado; sin importadores de `categoriaGastoOptions`/`CategoriaGasto` fuera de comentarios explicativos.
- [x] 8.3 Verificación manual en `npm run dev` — **confirmada por la usuaria** (re-seed de `schemaVersion` 3, tabla de gastos sin columna Categoría, alta de gasto/preventivo/correctivo `'otro'`, registro `gasto` del fixture visible sin sub-tipo, chips de alerta de service/VTV/RTO sin cambios, persistencia tras recargar).
- [x] 8.4 Verificación manual con una cuenta con solo `read` sobre `vehiculos` — **confirmada por la usuaria** (historial legible, alta deshabilitada y visible, no oculta).
- [x] 8.5 Tasks marcadas `[x]` (0-8 completas). Deviaciones encontradas documentadas en cada sección (ver especialmente 2.8, 4.1/4.2, 6.9) y resumidas en el reporte de `apply`. Progreso guardado en engram (`sdd/vehiculo-mantenimiento-registro/apply-progress`).
