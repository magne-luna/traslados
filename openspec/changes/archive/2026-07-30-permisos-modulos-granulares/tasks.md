## 1. Documentación de la discrepancia (antes de tocar código)

- [x] 1.1 Agregar entrada en `knowledge-base/04_modelo_de_datos.md` §Discrepancias documentando que el catálogo de módulos pasa de 4 a 7, que no es un pedido del cliente sino una decisión de UX confirmada con el usuario, y qué 3 módulos nuevos se separan de cuáles módulos padre.
- [x] 1.2 Agregar bullet `⚠️ Discrepancia con Traslados-Modelo-Datos.docx` en la entrada de este change en `CHANGES.md`.
- [x] 1.3 Agregar el componente `AvisoModeloDatos` (`frontend/src/design-system/components.tsx`) en la pantalla de Cuentas (`CuentaDetail.tsx` o `MatrizPermisos.tsx`, donde se vea la matriz de permisos), explicando la discrepancia a quien administra cuentas. Hecho en `CuentaDetail.tsx`, sección "Permisos" (visible tanto para cuentas admin como empleado). Test RED→GREEN en `CuentaDetail.test.tsx`.

## 2. Migración de base de datos (Supabase)

- [x] 2.1 Crear la migración `supabase/migrations/<timestamp>_split_modulos_permisos.sql`. Hecho: `supabase/migrations/20260730140000_split_modulos_permisos.sql`.
- [x] 2.2 `INSERT` de `hojas_de_ruta`, `vehiculos` y `presupuestos` en `modulos.modulos` (`ON CONFLICT (tipo_modulo) DO NOTHING`, mismo patrón que `20260728120000_seed_modulos.sql`).
- [x] 2.3 Copiar aditivamente `modulos.permisos`: toda fila con `modulo_id` de `pacientes` se duplica con `modulo_id` de `hojas_de_ruta` (mismo `nivel_acceso`); ídem `facturacion`→`presupuestos` y `conductores`→`vehiculos` (design.md D1). Verificar que las filas originales de los módulos padre no se tocan. Implementado con `INSERT ... SELECT ... ON CONFLICT (usuario_id, modulo_id) DO NOTHING` (además hace la migración idempotente); ningún `UPDATE`/`DELETE` sobre filas del módulo padre.
- [x] 2.4 `DROP POLICY IF EXISTS` + `CREATE POLICY` en `pacientes.recorridos` y `pacientes.historial_recorridos`, reemplazando `tiene_permiso('pacientes', ...)` por `tiene_permiso('hojas_de_ruta', ...)` (nombres de policy tal como están en `20260724100004_schema_pacientes.sql`: "Read recorridos", "Write recorridos", "Read historial_recorridos", "Write historial_recorridos").
- [x] 2.5 `DROP POLICY IF EXISTS` + `CREATE POLICY` en `facturacion.presupuesto` y `facturacion.autorizacion`, reemplazando `tiene_permiso('facturacion', ...)` por `tiene_permiso('presupuestos', ...)` (nombres: "Read presupuesto", "Write presupuesto", "Read autorizacion", "Write autorizacion"). `facturacion.facturas`, `facturacion.cobros` y `facturacion.gastos_vehiculos` NO se tocan — quedan en `facturacion`.
- [x] 2.6 `DROP POLICY IF EXISTS` + `CREATE POLICY` en `conductores.vehiculo`, `conductores.accesorios_vehiculo`, `conductores.documentacion_vehiculo`, `conductores.mantenimiento` y `conductores.conductores_vehiculos`, reemplazando `tiene_permiso('conductores', ...)` por `tiene_permiso('vehiculos', ...)` (nombres: "Read vehiculo", "Write vehiculo", "Read accesorios_vehiculo", "Write accesorios_vehiculo", "Read documentacion_vehiculo", "Write documentacion_vehiculo", "Read mantenimiento", "Write mantenimiento", "Read conductores_vehiculos", "Write conductores_vehiculos"). `conductores.conductores` y `conductores.documentacion_conductores` NO se tocan — quedan en `conductores`.
- [x] 2.7 Aplicada por la usuaria fuera de este sandbox (sin proyecto Supabase local/Docker/credenciales de staging disponibles acá para hacerlo desde el agente). Confirmado manualmente 2026-07-30: la migración corrió contra el entorno real. Durante la verificación se detectó un bug de lectura (no de la migración en sí, ver 6.1) que ya fue corregido — ver `SupabaseAuthRepository.ts`/`SupabaseCuentaRepository.ts` más abajo.

## 3. Tipos y configuración de permisos (frontend)

- [x] 3.1 `frontend/src/shared/types/usuario.ts`: expandir `Modulo` a `'pacientes' | 'hojas_de_ruta' | 'obra_social' | 'facturacion' | 'presupuestos' | 'conductores' | 'vehiculos'`.
- [x] 3.2 `frontend/src/features/cuentas/modulos.ts`: actualizar `MODULOS` (orden estable, 7 valores) y `ETIQUETA_MODULO` (agregar "Hojas de Ruta", "Presupuestos", "Vehículos"; ajustar "Pacientes", "Facturación", "Conductores" si hace falta aclarar que ya no incluyen el sub-módulo). No hizo falta aclarar las 3 etiquetas viejas — el nombre del módulo ya no ambiguo una vez que `SUBMODULOS_MODULO` se eliminó (3.3).
- [x] 3.3 Eliminar `SUBMODULOS_MODULO` y sus usos en `PermisosMatrizFields.tsx` (design.md D4). También se quitó el prop `hint` del `<Field>` (quedaba vacío sin la constante).
- [x] 3.4 Actualizar `MODULO_COLOR` a 7 entradas reutilizando el tono del módulo padre en el hijo (design.md D5): `hojas_de_ruta: 'success'`, `presupuestos: 'warning'`, `vehiculos: 'info'`.
- [x] 3.5 `frontend/src/app/routes.ts`: actualizar `APP_ROUTES` para que cada `AppRoute` apunte a su propio módulo (`/hojas-de-ruta` → `hojas_de_ruta`, `/presupuestos` → `presupuestos`, `/vehiculos` → `vehiculos`), y borrar los comentarios que describen la agrupación vieja.
- **Deviación (no en el plan original, encontrada al recorrer el código)**: `frontend/src/features/cuentas/moduloIcons.tsx` tiene un `Record<Modulo, ReactNode>` (`MODULO_ICON_PATH`) que no estaba listado en design.md/tasks.md pero es parte del mismo contrato de tipo `Modulo` — sin agregar sus 3 entradas nuevas, `tsc -b --noEmit` rompe. Se agregaron reusando exactamente los mismos paths que `app/navIcons.tsx` ya tenía para `hojasDeRuta`/`presupuestos`/`vehiculos` (mismo criterio que los 4 módulos originales).

## 4. UI de la matriz de permisos

- [x] 4.1 Revisar `MatrizPermisos.tsx` y `PermisosMatrizFields.tsx`: confirmar que el layout no está hardcodeado para 4 filas (grid, alturas fijas) y ajustar si hace falta para 7 filas. Confirmado: `grid-cols-1 md:grid-cols-2` con `gap-md`, sin alturas fijas ni conteo hardcodeado — no hizo falta ningún ajuste.
- [x] 4.2 Revisar `CuentaForm.tsx` (permisos iniciales de alta de cuenta): mismo chequeo de layout/inicialización para 7 módulos. `filasVacias()` ya deriva de `MODULOS`, sin hardcodear 4 — no hizo falta ningún ajuste.
- [x] 4.3 Revisar `CuentaDetail.tsx` (Chips de nivel por módulo): confirmar que itera sobre `MODULOS` sin asumir 4 elementos. Confirmado (`{MODULOS.map(...)}`, `flex-wrap`) — no hizo falta ningún ajuste.

## 5. Tests (Strict TDD — RED antes de tocar producción, GREEN, TRIANGULATE, REFACTOR)

- [x] 5.1 Safety net: correr la suite completa antes de tocar nada, registrar el baseline de tests pasando. Baseline: **188 archivos de test, 1253 tests, todos pasando** (previo a cualquier edición de este batch).
- [x] 5.2 `modulos.test.ts`: actualizar (o escribir primero, en rojo) los tests de `MODULOS`, `mapaAFilas`, `filasVacias`, `filasAPermisos`, `filasSonIguales` para 7 módulos. Reescrito completo — antes solo testeaba `MODULO_COLOR`/`SUBMODULOS_MODULO`, ahora también testea las 4 funciones puras directamente (antes solo cubiertas indirectamente vía tests de componente).
- [x] 5.3 `routes.test.ts`: actualizar los tests de `moduloDeRuta()` para las rutas que cambian de módulo (`/hojas-de-ruta`, `/presupuestos`, `/vehiculos`), agregando el caso de que ya no comparten módulo con su antiguo padre.
- [x] 5.4 `PermisosMatrizFields.test.tsx` y `MatrizPermisos.test.tsx`: actualizar para 7 filas, incluyendo un caso de asignar niveles distintos a un módulo padre y su hijo (ej. `pacientes: write`, `hojas_de_ruta: read`) para probar que quedaron desacoplados.
- [x] 5.5 `router.cuentas.test.tsx` y `router.test.tsx`: actualizar cualquier fixture de permisos que asuma 4 módulos. **Revisados, no tenían ninguna asunción de 4 módulos** — no requirieron cambios.
- [x] 5.6 `AppShell.test.tsx`: actualizar los tests de navegación filtrada por permisos para reflejar que tener `pacientes` ya no muestra "Hojas de Ruta" automáticamente (y análogo para los otros dos pares). Se agregaron además 3 tests de triangulación (permiso solo sobre cada módulo hijo nuevo).
- **Deviación (no en el plan original, encontrada al correr la suite completa tras 3.x/4.x — 8 tests rotos fuera de los archivos listados en 5.2–5.6)**: 3 archivos de tests de "coherencia entre rutas" de changes anteriores (`gateo-hojas-de-ruta`, `gateo-facturacion`, `gateo-conductores`, todos ya archivados) codificaban como aserción la agrupación VIEJA de módulos — probaban explícitamente que dos rutas que hoy quedan separadas por este change *debían* compartir permiso. Con la separación 1:1 esas aserciones pasan a ser falsas por diseño (es justamente el comportamiento nuevo que pide este change). Se reescribieron los 3 archivos para afirmar la independencia de módulos en vez de la agrupación vieja, agregando el caso "escritura en el módulo viejo ya no habilita la pantalla nueva" (alineado con los escenarios de `specs/permisos-modulo-frontend/spec.md`):
  - `frontend/src/features/hojas-de-ruta/HojaDeRutaPage.coherencia.test.tsx` (pacientes/hojas_de_ruta)
  - `frontend/src/features/facturacion/PresupuestosFacturacionCoherencia.test.tsx` (facturacion/presupuestos)
  - `frontend/src/features/conductores/ConductoresPage.test.tsx` (conductores/vehiculos, describe "Coherencia del permiso...")
  No se tocó ningún componente de producción de esas features — el fix fue enteramente de expectativas de test, consistente con `moduloDeRuta()`/`APP_ROUTES` ya resueltos en 3.5.
- [x] 5.7 Correr la suite completa, confirmar 0 regresiones fuera de los archivos tocados, y correr `npx tsc -b --noEmit` dentro de `frontend/` (regla dura del proyecto — nunca `tsc --noEmit` a secas). Resultado final: **188 archivos de test, 1272 tests, todos pasando** (1253 baseline + 19 tests nuevos netos); `npx tsc -b --noEmit` sin errores; `npm run lint` (oxlint) sin errores nuevos (mismos 13 warnings preexistentes de `react(only-export-components)`, ninguno en archivos tocados por este change).

## 6. Verificación final

- [x] 6.1 Confirmado manualmente por la usuaria 2026-07-30, después de aplicar la migración: una cuenta con acceso previo sigue teniendo acceso a los módulos hijo correspondientes con el mismo nivel, para los 3 pares. **Deviación encontrada durante esta verificación**: el cambio de accesos a los módulos nuevos no se reflejaba en la UI — causa raíz: `isModulo()` en `frontend/src/shared/lib/auth/SupabaseAuthRepository.ts` y `frontend/src/shared/lib/cuentas/SupabaseCuentaRepository.ts` seguían con la lista literal vieja de 4 módulos (no derivaban del tipo `Modulo`), así que descartaban en silencio cualquier fila de `modulos.permisos` de los 3 módulos nuevos al leer — la escritura vía `update-permisos` siempre funcionó bien. Corregido con TDD (test rojo reproduciendo el mapa de permisos vacío → fix → verde) en ambos archivos; suite completa 1274/1274 tests, `tsc -b --noEmit` limpio. Guardado en engram `sdd/permisos-modulos-granulares/apply-progress`.
- [x] 6.2 Confirmar que la Edge Function `update-permisos` acepta los 3 módulos nuevos sin cambios de código (ya lee el catálogo dinámicamente — ver design.md Context). Confirmado por lectura del código de la función (no requiere entorno real): lee `modulos.modulos` dinámicamente y valida `body.permisos[].modulo` contra ese resultado — ningún nombre de módulo hardcodeado. Sin cambios de código necesarios, condicionado a que 2.7 se aplique primero (design.md D3: DB antes que frontend).
- [x] 6.3 Marcar las tasks completadas y dejar constancia en el change de cualquier desviación encontrada durante la implementación. Ver deviaciones anotadas en 2.7, 3.x y 5.x de este archivo, y el resumen completo devuelto al orquestador.
